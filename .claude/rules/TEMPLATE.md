# rules/{tech}.md 작성 가이드

> 이 파일은 기술 스택 규칙 파일을 작성할 때 참조하는 **메타 템플릿**입니다.
> 새로운 rules/{tech}.md를 생성할 때 이 구조와 기준을 따르면, 모든 에이전트가
> 해당 기술의 **시니어 개발자 수준**으로 동작하고, 바이브 코딩 시 **최고 품질의 결과물**을 보장합니다.

---

## 작성 원칙

### 1. 바이브 코딩 최적화
- 규칙은 **구체적이고 실행 가능**해야 합니다. "좋은 코드를 작성하세요" ❌ → "컴포넌트는 단일 책임 원칙에 따라 하나의 역할만 담당. 200줄 초과 시 분리" ✅
- AI가 코드를 생성할 때 **즉시 적용할 수 있는 수준**으로 작성합니다.
- "이렇게 하지 마세요" (안티패턴)을 반드시 포함합니다 — AI가 흔히 생성하는 나쁜 패턴을 방지합니다.

### 2. 시니어 수준 전문성
- 공식 문서에 나오는 기본 내용만 쓰지 않습니다.
- **실무에서 겪는 함정, 성능 이슈, 보안 취약점**을 포함합니다.
- "왜 이렇게 해야 하는지" 이유를 간결하게 덧붙입니다.
- 해당 기술의 **최신 권장 사항**을 반영합니다.

### 3. 꼼꼼하고 상세하게
- 빠뜨리는 영역이 없도록 아래 템플릿의 모든 섹션을 채웁니다.
- 각 섹션에 최소 3~5개의 구체적 규칙을 포함합니다.
- 애매한 표현 대신 **명확한 기준**을 제시합니다.

### 4. 프로젝트 독립성
- 특정 프로젝트에 종속된 내용을 넣지 않습니다.
- 해당 기술 스택을 사용하는 **모든 프로젝트**에 적용 가능해야 합니다.
- 프로젝트별 설정은 CLAUDE.md에 위임합니다.

---

## 템플릿 구조

```markdown
---
paths:
  - "{해당 기술의 파일 패턴}"
  - "예: **/*.ts, **/*.tsx, **/*.py 등"
---

# {기술명} 개발 규칙

> 이 파일은 {기술명} 프로젝트에 범용적으로 적용되는 시니어 수준 규칙입니다.
> 프로젝트별 설정, 빌드 명령, 폴더 구조는 `/CLAUDE.md`에 정의합니다.

## 핵심 원칙
{이 기술의 철학과 핵심 설계 원칙 3~5개}
{왜 이 원칙이 중요한지 한 줄 설명}

---

## 프로젝트 구조
{권장 폴더/파일 구조}
{파일 배치 규칙, 네이밍 규칙}
{모듈/컴포넌트 분리 기준}

## 네이밍 컨벤션
{변수, 함수, 클래스, 파일, 폴더 네이밍 규칙}
{해당 기술 커뮤니티의 표준 컨벤션}

## 코드 구조 & 패턴
{권장 패턴과 구체적 적용 방법}
{컴포넌트/모듈/클래스 설계 원칙}
{상태 관리 패턴}
{의존성 주입/관리}
{비동기 처리 패턴}

## 안티패턴 (하지 말 것)
{AI가 흔히 생성하는 나쁜 패턴 목록}
{각 안티패턴이 왜 나쁜지 이유}
{올바른 대안 제시}

## 에러/예외 처리
{해당 기술의 에러 처리 베스트 프랙티스}
{에러 경계, 폴백 전략}
{로깅 패턴}

## 보안
{해당 기술에서 주의할 보안 취약점}
{입력 검증, 인증/인가}
{민감 정보 관리}
{OWASP 관련 항목}

## 성능 최적화
{해당 기술의 성능 함정}
{메모리 관리, 렌더링 최적화}
{번들 사이즈, 로딩 최적화}
{캐싱 전략}

## 테스트
{권장 테스트 전략 및 도구}
{유닛/통합/E2E 테스트 기준}
{테스트 네이밍 및 구조}

## 스타일링 (UI 기술인 경우)
{스타일링 방법론}
{반응형 디자인 기준}
{테마/디자인 시스템 적용}
{접근성 (a11y) 기준}

## 빌드 & 배포
{빌드 설정 베스트 프랙티스}
{환경 변수 관리}
{배포 체크리스트}

## 자주 쓰는 라이브러리/도구
{해당 기술 생태계에서 검증된 라이브러리 추천}
{각 라이브러리의 용도와 선택 이유}
{피해야 할 라이브러리 (deprecated, 보안 이슈 등)}

## 프로젝트별 확장 포인트

> 아래 항목들은 프로젝트마다 다르므로 `/CLAUDE.md`에서 정의합니다:
> - 프로젝트 구조 상세
> - 빌드/실행 명령
> - CI/CD 설정
> - 외부 서비스 연동
> - 환경별 설정 (dev/staging/prod)
```

---

## 품질 체크리스트

rules 파일 작성 완료 후 아래를 확인합니다:

- [ ] **구체성**: 모든 규칙이 "즉시 코드에 적용 가능한" 수준인가?
- [ ] **안티패턴**: AI가 흔히 범하는 실수를 방지하는 항목이 있는가?
- [ ] **깊이**: 공식 문서 수준이 아닌, 실무 경험 기반 노하우가 포함되었는가?
- [ ] **최신성**: 해당 기술의 최신 버전/권장 사항이 반영되었는가?
- [ ] **완전성**: 위 템플릿의 모든 섹션이 채워졌는가? (해당 없는 섹션은 명시적으로 제외)
- [ ] **프로젝트 독립성**: 특정 프로젝트에 종속된 내용이 없는가?
- [ ] **일관성**: 다른 rules 파일과 용어/형식이 통일되었는가?
- [ ] **바이브 코딩 최적화**: AI가 이 규칙만 읽고도 시니어 수준 코드를 생성할 수 있는가?

---

## 작성 순서 권장

여러 기술 스택의 rules를 생성할 때:

1. **언어 규칙 먼저** (예: `typescript.md`, `python.md`)
2. **프레임워크 규칙** (예: `react-native.md`, `fastapi.md`)
3. **인프라/서비스 규칙** (예: `supabase.md`, `firebase.md`)

언어 규칙이 프레임워크의 기반이 되므로, 이 순서를 따르면 일관성이 유지됩니다.
